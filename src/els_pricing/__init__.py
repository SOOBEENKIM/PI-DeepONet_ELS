"""els_pricing — independent from-scratch reimplementation of the ELS fair-value
fast-pricing pipeline (data prep -> MC theoretical value -> DeepONet surrogate ->
margin MLP -> metrics / speed / case analysis).

See the design document (ELS_독립재구현_지시서.md) for the full specification.
"""

__version__ = "0.1.0"
